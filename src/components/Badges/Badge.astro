---
import "./Badge.css"

export interface Props {
    text: string;
    href?: string
}
const { text, href} = Astro.props
---

<li class="badge">
    <slot/>
    {href
        ? <a href={href}>{text}</a>
        : <p>{text}</p>
    }
</li>
