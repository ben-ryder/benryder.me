---
import {apiClient} from "../lib/api/api-client";

const header= await apiClient.getHeader()

---

<header>
		{header.messagePromoHtml &&
			<div class="promo-banner" set:html={header.messagePromoHtml} />
		}
		<noscript>
				<div class="no-script-banner" set:html={header.messageNoScriptHtml} />
		</noscript>
		<div class="top-bar">
        <div class="top-bar__logo">
            <p><a href="/">Ben Ryder</a></p>
        </div>
        <nav class="top-bar__navigation">
            <ul class="top-bar__menu">
							{header.navigationLinks.map(link => (
									<li class="top-bar__menu-item">
											<a class="top-bar__meni-link" href={link.url}>{link.text}</a>
									</li>
							))}
            </ul>
        </nav>
		</div>
    <hr />
</header>
