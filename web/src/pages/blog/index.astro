---
import {type CollectionEntry, getCollection} from "astro:content";

import GlobalLayout from "../../patterns/layouts/GlobalLayout/GlobalLayout.astro";
import CardComplexListing from "src/patterns/components/CardComplexListing/CardComplexListing.astro";
import {orderByTimestamps} from "../../utils/order-by-timestamps.ts";

const unorderedPosts = await getCollection('blog-posts', ({ data }) => {
	return data.status === 'published'
})
const posts = orderByTimestamps<CollectionEntry<'blog-posts'>>(unorderedPosts)
---

<GlobalLayout title="Blog Posts">
	<CardComplexListing
		title="🤔 Blog Posts"
		description="<p>Thought pieces, reflections and anything else random I fancy writing about.<br/>If you're interested in technical guides and how-tos, see my <a href='/guides'>guides</a> instead.</p>"
		notFoundText="No Guides Found"
		tags={[]}
		cards={posts.map(post => {
			return {
				title: post.data.name,
				description: post.data.description,
				link: `/blog/${post.id}`,
				tags: [],
				createdAt: post.data.createdAt,
				updatedAt: post.data.updatedAt,
			}
		})}
	/>
</GlobalLayout>
